name: CD

on:
  push:
    branches:
      - master

jobs:
  make:
    name: Make
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Test Build Publish
        run: make
        env:
          NPM_TOKEN: ${{ secrets.NPM_TOKEN }}